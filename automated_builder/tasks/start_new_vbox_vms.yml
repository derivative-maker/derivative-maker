---
- name: Register VMs
  shell: "VBoxManage list vms"
  register: existing_vms 

- name: Start gateway VM
  shell: "VBoxManage startvm Whonix-Gateway-XFCE --type=headless"
  when: "'Whonix-Gateway-XFCE' in existing_vms.stdout"

- name: Delete workstation VM
  shell: "VBoxManage startvm Whonix-Workstation-XFCE --type=headless"
  when: "'Whonix-Workstation-XFCE' in existing_vms.stdout"
