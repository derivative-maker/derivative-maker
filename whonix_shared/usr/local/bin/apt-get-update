#!/bin/bash

# Required to run apt-get update as user (non-root).
# Requried for whonixcheck function check_operating_system.
# Exception to run /usr/local/bin/apt-get-update as user
# is definied in /etc/sudoers.

apt-get update

exit "$?"

# End of Whonix /usr/local/bin/apt-get-update.