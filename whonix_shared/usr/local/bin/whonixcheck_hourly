#!/bin/bash

set -x

## Debugging:
#echo "whonixcheck_hourly pre: date: `date`" >> /home/user/delayed

## Debugging:
#sudo -u user bash -x 2>/home/user/whonixchecklog /usr/local/bin/whonixcheck -cron

sudo -u user /usr/local/bin/whonixcheck -cron &

## Debugging:
#echo "whonixcheck_hourly post: date: `date`" >> /home/user/delayed

exit 0

## End of Whonix /usr/local/bin/whonixcheck_daily.
