# 1. grml ok
# 2. vboxmange convertfromraw ok
# 3. copyinto ok-tg
# 4. chroot
# 5. run inside chroot
# 6. unchroot
# 7. create virtual machine
# 8. export vm

# rc.local hack no longer required?

grub-install in change hdd uuids script

# TODO CHROOT

# TODO DESKTOP
getty autologin on gateway

grep -r "update-grub2" *


 cd /home/user/Whonix

 grep -r "TODO" *

 grep -r "TODO CHROOT" *
 
 grep -r "TODO DOC" *
 
 grep -r "TODO DESKTOP" *
 
 grep -r "REVIEW" *
