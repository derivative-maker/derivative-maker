# aos /etc/sudoers changes.

# Possibly DANGEROUS! Needs to be audited!
# Race condition, syntax error detection do not apply here and we set correct permission just to make sure.

# For convenience.
# VMONLY
user ubuntu=NOPASSWD: /sbin/shutdown -h now,/sbin/reboot,/sbin/poweroff 

# /usr/local/bin/aoscheck depends on the following line for check_operating_system
# allows running apt-get update as user (non-root)
user ubuntu=NOPASSWD: /usr/local/bin/apt-get-update

# Allow Network Time Synchronization without password.
user ubuntu=NOPASSWD: /etc/init.d/htpdate

# End of aos /etc/sudoers changes.